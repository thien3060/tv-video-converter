{
  "name": "tv-converter",
  "productName": "TV Converter",
  "version": "0.1.2",
  "description": "Drop videos in, get Samsung-TV-safe MP4s out.",
  "main": "src/main/main.js",
  "author": "thien",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "electron .",
    "test": "node --test test/*.test.js",
    "fetch-ffmpeg": "node scripts/fetch-ffmpeg.mjs",
    "build:win": "node scripts/fetch-ffmpeg.mjs win32 && electron-builder --win",
    "build:mac": "electron-builder --mac"
  },
  "devDependencies": {
    "electron": "^38.0.0",
    "electron-builder": "^26.0.0"
  }
}
